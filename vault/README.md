# Sapient Vault

This directory is your Obsidian knowledge base.

After running `scripts/create-vault-symlink.sh`, it is symlinked to
`apps/web/content` so any file you save here appears immediately in the
web application.
